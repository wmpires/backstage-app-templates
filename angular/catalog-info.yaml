apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | dump }}
  description: Angular application
  tags:
    - angular
spec:
  type: app
  owner: user-experience # department
  lifecycle: tst # environment
  system: TWIZ # project
